package session01.inheritance.session1;

// abstract classes are being used like a template for child classes
//but, it is not allowed to create an object itself.
//because it's an abstract class (our template)

public abstract class BasePerson implements BaseSalary{

    String name;
    String surname;



}